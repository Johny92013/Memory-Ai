# DEPRECATED – nicht mehr aktiv

Diese Datei wurde am 2026-07-31 deaktiviert.

Aktiver Workflow: .github/workflows/release.yml (nur iOS IPA, signiert).

Grund: Doppelte iOS-Pipelines vermeiden; Android läuft lokal unter Windows/Cursor.
GitHub Actions führt nur noch Dateien *.yml / *.yaml unter .github/workflows/ aus.
Diese .deprecated-Datei wird nicht gestartet.
